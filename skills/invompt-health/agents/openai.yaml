interface:
  display_name: "Invompt Connection"
  short_description: "Check whether Invompt is connected and healthy"
  default_prompt: "Use $invompt-health to check whether Invompt is connected and healthy."
